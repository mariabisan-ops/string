#include <stdio.h>
#include <string.h>

int main (){
    char nome[50], sexo[50];
    int idade;
    
    printf ("Nome: ");
    fgets (nome, sizeof(nome), stdin);
    nome[strlen(nome)-1] = '\0';

    printf ("\nSexo: ");
    fgets (sexo, sizeof(sexo), stdin);
    sexo[strlen(sexo)-1] = '\0';

    printf ("\nIdade: ");
    scanf ("%d", &idade);
    
    if (strcmp (sexo, "feminino")==0 && idade<25){ //usa strcmp para comparação de uma string
        printf ("%s", nome);
        printf ("\nACEITO");
    }

    else {
        printf ("NÃO ACEITO");
    }
}
