#include <stdio.h>
#include <string.h>

int main(){
    char nome[50];
    printf ("Nome: ");
    fgets (nome, sizeof (nome), stdin);
    nome[strlen(nome)-1] = '\0';//para tirar o enter

    if (nome[0]=='a' || nome[0]=='A'){
        printf ("%s", nome);
    }
    else {
        printf ("não é possivel imprimir esse nome");
    }
}
